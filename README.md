# YouzaiWorldWebAssets
主站资源文件，用于同步到cloudflare上

资源公开地址：`https://assets.mcyzw.top/`。官网与 API 管理后台共用的图片、字体、图标、音视频及 PWA 图标统一放在 `public/` 下，前端通过该地址引用；官网首页的像素 Logo 也位于 `public/images/logo.png`，通过 CORS 支持 Three.js 读取像素。
